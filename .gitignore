# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# 本地工具链缓存（pnpm/cargo 缓存重定向到这里）
.cache/

# Tauri 生成的 schema（每次构建自动再生）
src-tauri/gen/schemas

# 视觉改版的设计稿与 QA 截图（本地留存，不入库）
design/

# 阶段性的里程碑设计稿 / 调研记录 / 验收清单（本地留存，不入库）
# 理由：这类文档天然滞后于代码，且与 M4 起的新决策存在冲突（如已取消的扫码配对、
# 已推翻的前台服务插件方案）；**以代码与 AGENTS.md 为准**。有长期价值的部分
# （Android 平台限制、环境坑、往返保真 bug 根因）由 AGENTS.md 承接。
docs/

# 智能体工作目录（本地记忆/技能，不入库）
.workbuddy/

# Rust 构建产物（数 GB）
src-tauri/target

# Editor directories and files
.vscode/
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
