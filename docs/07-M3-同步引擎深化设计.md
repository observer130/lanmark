# 07 · M3：同步引擎深化（自动同步 + 删除传播 + LWW 冲突裁决）

> 状态：**M3 验收通过（2026-09-20，真机 REDMI K90 / Android 16）**：M3a–M3e 已实施（见 §11）；M3f 真机走查通过，走查中发现并修复 1 项「手机目录不刷新远端删除」（§11f）。Rust 91/91 + 前端 87/87 + CI 三端绿（build #35504612769），§11e 清单逐项勾全。
> 前置：M2 真机验收通过（docs/05 §8h，REDMI K90 / Android 16）；review 修复批次落地（§8i，Rust 70/70 + 前端 73/73）。
> 铁律（docs/01 §3.6）仍是最高设计约束：**同步引擎永不静默删数据**——删除先进回收站，冲突输家必留底（可见副本），任何裁决不丢弃内容。
>
> 冲突模型基准（2026-09-19 调研结论）：云笔记类（有道/幕布/Notion）「从不冲突」= 中心服务器永远在线 + 细粒度自动保存 + 字段/块级 last-write-wins，是架构红利而非算法；文件同步类（Dropbox/Syncthing）= **mtime 较新者留原路径、较旧者保留为冲突副本**。Lanmark 中心节点（手机）会离线，属文件同步类，采用 **B 方案：mtime LWW + 可见冲突副本**，无需用户处理。

---

## 1. 目标与非目标

### 目标

1. **自动同步循环**（桌面客户端）：周期 / 保存后 / 前台恢复 / 上线恢复自动回合；开关联动（默认开），关闭时手动「立即同步」仍可用
2. **删除传播**（tombstone）：一端删除 → 另一端软删，铁律安全（内容未变才静默传播；删后编辑过则按 LWW 裁决）
3. **LWW 冲突裁决**：两端离线改同一文件 → **mtime 较新者自动留原路径**，较旧者自动保留为可见冲突副本（`<stem>-冲突<时间>`，在目录树里），用户无需任何操作
4. **多端收敛**：修复双客户端基线缺陷（§5 步骤 8），服务器侧 push 同步做 mtime 仲裁（§5.1），桌面×2 + 手机×1 无人值守收敛
5. **同步状态 UI**：在线状态、最近同步时间、合并报告（哪版赢、输家副本在哪）

### 非目标（推迟）

| 项 | 推迟到 | 说明 |
|---|---|---|
| 扫码配对（相机） | **M4**（用户定） | 手输/粘贴 URL 保持 |
| 自签 TLS + 证书固定 | M4 | docs/02 既定 |
| 版本历史 UI（查看 `.lanmark/trash` 与冲突副本归档） | 二期（docs/02 候选既有项） | M3 的可发现性靠「副本在树里 + 合并报告」 |
| notify 文件监听 | 二期 | 回合开头 reindex 已覆盖外部改动，延迟 ≤ 一个回合（docs/05 §1 非目标清单，用户确认） |
| 块级/增量同步、断点续传、多 vault、桌面当中心、CRDT | 二期 | 不变 |
| 自动同步周期可配置 | 二期 | M3 固定 60s |
| mDNS 连续监听（服务器 IP 变化自动修复） | M3 P2 | 探测循环为主轴；手输重配对兜底（本环境桌面多播本就不可达，docs/05 §8h） |
| ~~冲突 diff 页面 / 人工解析~~ | **取消**（2026-09-19 二轮决策，改 LWW 自动裁决） | 不引入 `@codemirror/merge`、不建冲突账本、不做解析命令 |

## 2. 用户决策记录

**第一轮（2026-09-19 早）**：① 扫码配对降 M4；② 设自动同步开关（默认开），关闭时手动可用；③ ~~冲突展示 diff 页面选保留~~（被第二轮取代）；④ tombstone 存储授权自由裁量 → 取文件 `.lanmark/tombstones.json`（与基线 `sync-<id>.json` 同模式；reindex 重建 DB，tombstone 不可从文件推导，属同步状态）；⑤ M3 不做 notify。

**第二轮（2026-09-19 晚，对标有道/幕布/Dropbox/Syncthing 调研后）**：

- **冲突模型采用 B 方案**：mtime LWW（较新者留原路径）+ 输家自动保留为**可见**冲突副本；删除/编辑分歧同规则裁决；无 diff 页面、无人工解析步骤
- 裁决时间源 = 文件 mtime（manifest 已带 `mtime_ms`；与 Syncthing 版本向量语义一致）。不用「服务器到达顺序」（Dropbox 方式）：对单人多设备，到达顺序取决于谁先上线，可能让旧版赢，违背「保存时间为准」的用户预期
- 可发现性策略：副本进目录树（用户可见）+ 同步报告列出合并明细；不建持久账本（二期版本历史 UI 再考虑）

## 3. 协议演进（v1 → v1.1，全部加法式、向后兼容）

| 变更 | 内容 | 兼容性 |
|---|---|---|
| 新端点 `GET /api/v1/tombstones` | 需鉴权；返回 `[{path, hash, mtimeMs, deletedAt}]` | 旧服务器 404 → 客户端按空列表处理，删除传播静默关闭（退化 M2 行为，不报错） |
| `PushResult` + `serverHash: Option<String>` | 该路径在服务器上**操作后的最终 hash**：干净落盘 = 所推 hash；被仲裁降级 = 原路径现有 hash | 新增字段，旧客户端忽略 |
| 服务器 push 仲裁（**语义变更，wire 不变**） | `PushFile.mtimeMs`（已有字段）从「仅记录」升级为「仲裁依据」：baseHash 不符时不再无条件冲突改名，按 §5.1 规则 mtime 仲裁 | 旧客户端行为不变（旧客户端推的 mtime 同样参与仲裁，只会更正确） |
| `SyncReport.conflicts: Vec<String>` → `Vec<MergeEvent>` | `MergeEvent {path, winner: "local"\|"server", loserCopy, winnerMtimeMs, loserMtimeMs}`；报告仅 IPC 消费（非 wire 字段） | 前端与 Rust 测试同步更新 |
| `/delete` 语义不变（服务器软删入回收站） | 服务器**同时记 tombstone**；路径被（重新）创建时**清除该路径 tombstone**（规则 D4） | 旧客户端调 /delete 行为不变 |
| `/info`、`/manifest`、`/pull` 不变 | — | — |

## 4. LWW 裁决规则（M3 的核心语义）

### 4.1 元组比较

版本元组 = `(mtime_ms, size, hash_hex)`，字典序，**大者赢**：

- mtime 不同 → mtime 新者赢（正常路径）
- mtime 相同（同毫秒，跨设备罕见）→ size 大者赢
- size 相同 → hash 字典序大者赢（hash 相同则内容相同，本不会进裁决）
- 删除参与裁决时，删除的元组取 `(tombstone.mtime_ms, 0, tombstone.hash)`——size 记 0，故 **mtime 平手时编辑恒赢删除**（偏保守，保数据在树里）

### 4.2 三种分歧的裁决

| 分歧 | 裁决 | 输家去向 |
|---|---|---|
| **edit/edit**（两端都改了同一文件，均 ≠ 基线） | mtime 新者留原路径 | 较旧者 → 冲突副本 `<stem>-冲突<mmdd-HHMM>[N]<ext>`（复用 M2 `write_conflict_copy`，进索引，随下回合同步到全端） |
| **edit/delete**（一端 tombstone，另一端有内容且 hash ≠ tombstone hash） | 按 §4.1 元组比较（删除元组 size=0） | 编辑赢 → 文件在删除侧复活；删除赢 → 文件入删除侧回收站（内容可找回） |
| **删除传播**（tombstone 与现存文件 hash 相同 = 删后没再改） | 不裁决，直接传播 | 双方回收站都有 |

**铁律核查**：所有分支输家内容都落盘（冲突副本 / 回收站），无静默丢失；静默动作（hash 相同的删除传播）不丢任何内容。

### 4.3 时钟偏移

mtime 依赖设备时钟。个人局域网设备通常 NTP 同步，偏移秒级以下；即使偏移导致「裁决方向反了」，输家仍留副本，**无数据丢失**，用户可在副本里找回。可接受，文档钉住。

## 5. 回合语义 v2（客户端）

编号沿用 docs/05 §8b（M2 回合）：

0. 本地 reindex（不变，磁盘为事实源）
1. 拉 manifest + **tombstones（新）** + 本地清单 + 基线
2. 四分类（不变：仅服务器有→拉 / 仅本地有→推 / hash 同→跳 / hash 异→三方对比）
3. **（新）服务器 tombstone 处理**：hash 同 → 本地软删 + 记本地 tombstone（`report.deleted++`）；hash 异 → 按 §4.2 edit/delete 裁决（编辑赢 → push 本地 P 复活，服务器按 D4 清 tombstone；删除赢 → 本地软删 + tombstone）；本地无 P → 补记 tombstone 跳过
4. **（新）本地 tombstone vs 服务器 manifest**：镜像 §5 步骤 3（方向对调；删除赢 → 调 `/delete`）
5. **edit/edit 冲突（LWW 裁决，取代 M2「本地恒赢」）**：
   - 本地 mtime 新 → M2 既有行为：服务器版本拉为本地冲突副本，本地 P 以 baseHash=服务器现 hash 推送（服务器侧 §5.1 仲裁保证干净落地）
   - **服务器 mtime 新（M2 没有的方向）→ 先把本地当前字节存为冲突副本，再 pull 服务器 P 覆写本地**（副本命名幂等：失败重跑产生 `-2`，不丢不重）
   - 平手 → §4.1 元组；两个客户端各自独立裁决可能方向相反 → 服务器仲裁 + 基线修复保证 **≤2 个回合收敛**（无丢失）
   - 每个裁决产生一个 `MergeEvent`
6. 拉取（不变，批量 32；**pull 成功的路径清除本地对应 tombstone**，D4）
7. 推送（不变，批量 8；**服务器对干净落盘/仲裁获胜的路径清除对应 tombstone**，D4）
8. **基线（G3 修复）**：= 回合开始时的 manifest，按本回合结果更新——拉取路径 → 实拉 hash；推送路径 → **`serverHash`**（被仲裁降级时 = 原路径 hash）。堵住多端缺陷：M2 在「push 被服务器改名」后仍把自己 hash 写进基线，下一轮误判「只有服务器改过」→ 快进拉取覆盖本地版本（内容仅存于副本）
9. 持久化：tombstones（TTL 清理）+ 基线 + 报告

### 5.1 服务器侧 push 仲裁（新增）

服务器处理 push、`baseHash` 与现状不符（竞态：另一客户端刚改过）时，不再无条件把来件冲突改名，改为按 §4.1 元组与**当前 P** 比较：

- 来件元组更大 → 来件赢：当前 P 存为服务器侧冲突副本（`write_conflict_copy`），来件落 P
- 来件元组更小/平手 → 来件冲突改名（M2 既有行为），当前 P 保留

`serverHash` 始终返回操作后 P 的最终 hash。此规则让「推送到达顺序 ≠ 保存时间顺序」的竞态也收敛到正确的 LWW 结果，且避免旧版赢 + 下回合再翻案产生的冗余副本。

**序列化前提不变**：服务器 runtime 单线程、handler 天然串行，仲裁的检查-落盘无竞态（M2 既有前提）。

## 6. 可发现性（替代人工解析的设计）

- **副本在树里**：冲突副本是普通笔记（进索引、可搜索、可打开、参与同步）——用户一眼可见「有个旧版本」，想合并就手动合并后删副本，不管它就静静躺着
- **合并报告**：`SyncReport.conflicts` → `Vec<MergeEvent>`，SyncSection 展示「自动合并 N 项（较新留原名）」+ 逐条「`工作/笔记.md` → 副本 `…-冲突0920-1030.md`（保留 10:30 版）」，点击副本路径打开对应笔记
- **删除传播**：`report.deleted` 计数 + 列表，文案「已同步删除 N（进回收站）」
- 手机端（服务器面板）：库中冲突副本计数（扫描 `冲突` 命名笔记）+ 最近回合时间；手机端无客户端循环，裁决只发生在客户端回合中，服务器侧仲裁的副本同样进树可见

## 7. 自动同步循环（TS 驱动；仅客户端——手机是服务器，无循环）

### 设置

- `syncAuto: bool`（默认 **true**）：`app_config_dir/config.json` 的 `AppConfig` 加字段（serde default true，旧配置兼容）
- 开关在桌面客户端面板（SyncSection）；关闭 = 停止定时器/探测/自动回合，仅保留手动「立即同步」
- 文案注明：桌面应用完全关闭后无后台同步（app 模型，非常驻；手机侧才是常驻中心）

### 触发器（开关开时）

| # | 触发 | 回合时机 |
|---|---|---|
| A | 周期探测：对每个已配对服务器 `GET /info`（无鉴权轻量），在线状态 60s 至多 1 回合 | 在线且距上回合 ≥60s → 回合 |
| B | 离线退避：探测失败 → 该服务器下轮探测间隔 1s→2s→4s→…→60s 封顶；离线→在线跳变 | 跳变时立即回合 |
| C | 保存完成：vault store 保存成功后经轻量 `CustomEvent` 通知（避免两 store 循环依赖），5s 内无新保存 | 5s 后（尾沿纪律，与 M2 P0「不打字中途同步」一致）——**这是「商用无冲突感」的主要来源：在线时分歧窗口从小时级压到秒级** |
| D | 应用回前台（visibilitychange / focus） | 立即探测 + 回合门控 |
| E | 手动「立即同步」 | 立即（恒可用，不受开关限制） |

- 所有触发走同一入口：尊重 `syncing` 串行锁；C/E 保留 M2「同步前 flush 脏编辑」
- vault 未打开（unconfigured）→ 循环空转
- mDNS 连续监听（P2）：连续探测失败 3 次后 browse 一次，发现同名服务器则更新 profile url 并触发回合；不实现则手输重配对兜底

### 探测实现

Rust 新增命令 `sync_probe(serverId) -> {online, name, notes, assets}`（GET /info，3s 超时）；TS 循环状态机 per-server 记录 `{lastProbeAt, failCount, online, lastRoundAt}`。

## 8. 同步状态 UI（桌面）

- 服务器卡片：在线/离线（探测状态机）+「最近同步 HH:mm:ss」（profile JSON 加 `lastSuccessAt`，serde default，重启不丢）
- 报告区：拉/推/跳过/删除 N/自动合并 N（MergeEvent 明细，副本路径可点击）
- 不建冲突列表/账本 UI（副本在树里，M2 的「冲突 N」行改为「自动合并 N」）

## 9. 模块拆分与实现顺序（每步独立可测）

| 步 | 内容 | 层 |
|---|---|---|
| M3a | G3 基线修复：`PushResult.serverHash` + 回合步骤 8 + **服务器侧 push mtime 仲裁**（§5.1）+ **双客户端集成测试**（三端收敛：顺序/交错/同文件并发竞态，断言 LWW 胜者与副本落点） | Rust |
| M3b | tombstone：`/tombstones` 端点、服务器 `/delete` 记录、D4 清除规则、回合步骤 3/4（含 edit/delete LWW 裁决）、TTL 30 天、文件夹展开、`report.deleted`、旧服务器 404 退化 | Rust + 协议 |
| M3c | 客户端 LWW 冲突分支：「服务器更新 → 本地先存副本再 pull」方向 + `MergeEvent` 报告 + SyncSection 文案 | Rust + TS |
| M3d | 自动同步循环：`sync_probe` 命令、`syncAuto` 配置字段、TS 状态机（触发器 A–E）、开关 UI | Rust + TS |
| M3e | 同步状态 UI：在线状态、`lastSuccessAt`、合并报告展示 | TS |
| M3f | 真机走查 + 验收（§10）+ 实现记录（补本文 §11） | 全 |

### 测试账（计划）

**Rust**（M2 的 70 例全部保留）：

- M3a：三端回合（2 客户端 + 1 服务器）——顺序收敛 / 交错收敛 / 同文件并发竞态（推送到达顺序 ≠ mtime 顺序时，**最终 P = mtime 新者**，输家副本落点正确）；服务器仲裁单测（来件新/来件旧/平手三向）
- M3b：tombstone 场景——hash 匹配传播 / 删后编辑（edit/delete 裁决：编辑新→复活、删除新→软删、平手→编辑赢）/ 双方已删跳过 / D4 重新创建清除 / TTL 清理 / 文件夹展开 / 旧服务器 404 退化
- M3c：客户端 LWW 双向（本地新→M2 路径回归；服务器新→先副本后 pull，pull 失败时副本不丢）+ `MergeEvent` 断言
- 铁律审计：每个裁决/传播分支断言「输家内容必落盘（副本或回收站）」

**前端**（M2 的 73 例全部保留）：循环状态机（触发门控/退避/串行/关开关仅手动）、报告渲染、开关持久化

**真机**：按 §10 走查。

## 10. 验收标准（M3）

1. **三端收敛**：桌面×2 + 手机×1 各自改动（含附件），自动循环（不点任何按钮）≤3 个回合三端一致
2. **离线自动同步**：飞行模式 → 改 3 篇 → 恢复网络 → ≤2 个探测周期内自动同步，三端互见
3. **LWW 裁决**：双端离线改同一篇（保存时间不同）→ 同步 → 较新版在两端原路径、较旧版为可见冲突副本（内容完整），**全程无人工操作**；报告列出合并明细；次回合收敛（副本三端互见）
4. **删除传播**：桌面删一篇 → 同步 → 手机与桌面 2 该笔记均消失（三端回收站均有）；随后从回收站恢复并编辑 → 同步 → 按 LWW 裁决（编辑时间 vs 删除时间），无手动决策
5. **开关**：关自动同步 → 5 分钟内无回合（日志观测），手动按钮有效；再开 → ≤60s 恢复回合
6. **服务器恢复**：杀手机 App 进程（FGS 重启、自动开库复活）→ 桌面探测退避、≤60s 内恢复并自动完成一轮
7. `pnpm test` + `cargo test` 全绿；CI 三端绿；真机走查（REDMI K90 / Android 16）

## 11. 实现记录（2026-09-20，M3a–M3e）

### 11a. 代码结构（M3 增量）

| 模块 | 内容 |
|---|---|
| `sync.rs` | 协议 v1.1：`PushResult.serverHash`（操作后最终 hash，`serde default` 兼容旧服务器）；`version_gt` 版本元组比较（§4.1）；`meta_mtime_ms`；tombstone 存储层（`.lanmark/tombstones.json`，load/save/record/clear/TTL 30 天）；`MergeEvent` + `SyncReport.merges`/`deleted`（仅 IPC） |
| `sync_server.rs` | **`arbitrate_existing` 服务器侧 push 仲裁（§5.1）**：当前 P 存在且 hash ≠ baseHash（含 base 空 + 文件已存在，堵 M2 静默覆盖洞）→ 版本元组裁决：来件新 → 原版本成冲突副本、来件落原路径；来件旧/平手 → 来件降级、原路径保留；笔记与附件同规则。**LWW 时间源保真**：push 落地保留所推 `mtime_ms`（见 11d 事故与修复）。`/tombstones` 端点（需鉴权 + 惰性 TTL 清理）；`/delete` 经 `entry_delete_op` 自动记服务器 tombstone；`sync_probe`（GET /info，3s 超时，永不抛错）；`sync_conflict_count`（DB LIKE + assets 浅扫）；`AppState.last_sync_round_at`（/manifest 记最近回合） |
| `sync_client.rs` | 回合重构为分阶段 `RoundCore`（new → server_tombstones → local_tombstones → resolve_conflicts → pull → push → finish；测试可阶段间注入构造竞态）。**步骤 5 LWW（§4.2）**：本地新/平手 → M2 路径（服务器版成本地副本 + 推送）；服务器新 → 先存本地副本再拉取覆写。**步骤 8 基线（G3）**：= 回合开始 manifest，按实拉 hash / serverHash 更新；推送被仲裁降级 → 回合内纠正拉取（先拉验 hash 再覆写本地），失败兜底基线=所推 hash（下回合快进，内容在服务器副本中安全）。**tombstone 阶段（§5 步骤 3/4）**：服务器 tombstone → 本地软删/编辑复活；本地 tombstone → /delete 传播/拉取复活；回合开头 D4 判定（路径重现于磁盘：mtime 新于删除 = 复活，旧于删除 = 陈旧恢复再软删） |
| `fs_ops.rs` | `set_file_mtime`（LWW 时间源保真）；`entry_file_list`（文件夹展开：DB 笔记 + assets walk，符号链接不跟随）；`write_note`/`write_note_bytes`/`create_note`/`save_asset` 落盘即清 tombstone（D4，best effort + 回合开头权威判定兜底）；`rename_entry`/`move_entry` 记旧路径 tombstone + 清新路径（重命名 = 旧路径删除传播 + 新路径推送） |
| `commands.rs` / `vault.rs` | `entry_delete_op` 删除前记 tombstone（手机 UI 与服务器 /delete 同一入口）；`AppConfig.sync_auto`（默认 true，serde default 兼容旧配置；开库不再重置既有配置字段）；`vault_set_sync_auto`；`VaultStatus.syncAuto` |
| `src/lib/sync.ts` + `src/stores/sync.ts` | **自动同步循环状态机（§7，TS 驱动，仅桌面）**：1s tick + 重入保护；per-server `{online, failCount, lastProbeAt, lastRoundAt, mdnsRetried}`；触发 A（在线 60s 探测 + 回合门控）/ B（离线退避 1s→60s、恢复跳变立即回合）/ C（`lanmark:vault-saved` CustomEvent + 5s 尾沿）/ D（visibilitychange/focus 强制探测）/ E（手动恒可用）；所有触发走 `runRound` 同一入口（syncing 串行锁 + 同步前 flush 脏编辑 + 自动触发的「进行中」拒绝静默）；P2 mDNS 重发现（连续失败 3 次 browse，`sync_server_set_url` 修 url）；`syncAuto` 开关（`AppConfig` 持久化）+ `startAutoLoop/stopAutoLoop`（App 按 桌面+vault 就绪+开关 挂载） |
| `SyncSection.tsx` | 自动同步开关（含「应用完全关闭后无后台同步」文案）；服务器卡片在线状态点 + 上次同步时间；报告区「自动合并 N（较新留原名）」逐条明细（副本路径可点击打开 + 保留版本时间）+「已同步删除 N（进回收站）」；手机端面板最近回合时间 + 库中冲突副本计数 |

### 11b. 回合 v2 实现语义（§5 编号）

0. 本地 reindex（不变）
1. manifest + **服务器 tombstones（404 → 空列表退化）** + 本地清单 + 基线 + 本地 tombstone（TTL 清理后）
   - **回合开头 D4 判定**：本地 tombstone 的路径重新出现在磁盘（回收站恢复/重建）→ mtime 新于删除时刻 = 复活（清 tombstone，正常流程推送）；mtime 旧于删除时刻 = 陈旧恢复（再软删，删除仍有效）。此判定先于清单构建，保证分类看到的是判定后的磁盘
2. 四分类（不变）
3. **服务器 tombstone 处理**：服务器 manifest 仍有该路径（不一致态）→ 跳过防御；本地有且 hash 同 → 本地软删 + 记本地 tombstone；hash 异 → edit/delete LWW（删除元组 size=0）：编辑赢 → 推送复活 + 清陈旧本地 tombstone，删除赢 → 本地软删 + 继承服务器删除事件；本地无 → 补记跳过
4. **本地 tombstone 处理**（方向对调）：服务器有且 hash 同 → /delete 静默传播（**同时撤掉分类的 to_pull**——M2 会把已删文件快进拉回复活）；hash 异 → 服务器编辑新 = 拉取复活 + 清本地 tombstone，删除新 = /delete；服务器无 → 无操作
5. **edit/edit LWW**（取代 M2 本地恒赢）：元组比较；平手偏本地（两客户端可能各自方向相反 → 服务器仲裁 + 基线 ≤2 回合收敛，§12）
6. 拉取（不变；实拉 hash 进基线；**pull 落地保留服务器 mtime**）
7. 推送（不变；**基线取 serverHash**；降级 → 纠正拉取 + MergeEvent；来件获胜 → MergeEvent）
8. 基线 = 回合开始 manifest 按 6/7 结果更新（G3 修复）
9. 持久化：tombstones + 基线 + 报告；`lastSuccessAt` 由 Rust `sync_now` 成功时写 profile JSON

### 11c. 实现与设计稿的差异/补充

1. **mtime 跨端保真（设计稿 §2 的落地补全）**：设计稿钉「裁决时间源 = 文件 mtime，不用到达顺序」，但未钉**落地时保留 mtime**。实现中发现 M2 的 `write_note`/`fs::write` 落地会把服务器/本地文件的 mtime 重写成落地时刻——服务器侧实际退化为到达顺序仲裁：A 10:00 改（离线）、B 10:05 改，A 先推送落服务器（服务器 mtime=11:00），B 后推（10:05）→ 仲裁判 10:05 < 11:00，**较新的 B 被降级**——恰是 §2 拒绝的失败模式。修复：`fs_ops::set_file_mtime`，服务器 push 落地 + 客户端 pull 落地都保留源端 `mtime_ms`（无效值 ≤0 跳过）。补回归测试 `server_mtime_preservation_lww_not_arrival_order`（到达顺序 vs 保存顺序分离场景）
2. **重命名/移动传播**（设计稿未列）：`rename_entry`/`move_entry` 记旧路径 tombstone + 清新路径 → 对端收敛为「旧路径删除 + 新路径推送」，内容在新路径保留（铁律）。不传的话对端旧路径永久残留（M2 既有缺口，同删除不传播的根源）
3. **附件同规则仲裁**：§5.1 的表述是通用的（「与当前 P 比较」），M2 的附件 push 无冲突检查（同路径不同内容静默覆盖）。实现统一：笔记/附件同走 `arbitrate_existing`
4. **回合开头 D4 判定独立成步**：设计稿的 D4 只说「路径被（重新）创建时清除」。实现拆成两处：写路径即时清（best effort）+ 回合开头权威判定（处理回收站恢复这种**不经过写路径**的重现，并按 mtime 区分复活 vs 陈旧恢复）。陈旧恢复若只按 D4 清除会把「删了又恢复没改」的文件错误复活
5. **测试端口段 4180..4199 → 4180..4239**：M3 测试矩阵（80+ 常驻测试服务器）并行耗尽窄段

### 11d. 测试账（2026-09-20）

**Rust 90/90**（M2 70 全部保留 + 20 新增）：

- M3a：`version_tuple_ordering`（元组三向）；`arbitrate_mtime_directions_and_tie`（仲裁三向：来件新/旧/平手 hash 序 + base 匹配不仲裁 + 文件不存在不仲裁）；三端顺序收敛（双客户端×手机，互见 + 终轮全跳过）；双端交错同文件较新者赢（本地赢方向 + 副本三端传播）；**并发竞态（阶段注入）**：A 旧版推送晚于 B 新版落服务器 → 服务器降级 → 回合内纠正拉取 → 基线 = serverHash（G3 直测）→ ≤2 回合三端收敛，P = mtime 新者
- M3b：hash 匹配传播（三端软删 + 回收站都有 + 不复活）；edit/delete 三向（编辑新 → 复活 + D4 清 tombstone；删除新 → 软删 + 回收站保留；平手 → 编辑赢）；双方已删跳过；D4 重建清除；TTL 清理；文件夹展开（含**根级附件不被前缀误删**的反向断言）；陈旧恢复不复活；恢复后编辑复活；重命名传播；旧服务器 404 退化（空路由 axum）
- M3c：`client_lww_server_newer_saves_copy_then_pulls`（服务器更新方向全流程：先副本后拉取、不推旧版、事件断言、次回合副本三端互见）；`server_mtime_preservation_lww_not_arrival_order`（§2 回归）；降级/获胜分支的 MergeEvent 断言
- M3d：`probe_server_online_and_offline`（探测永不抛错）；`config_roundtrip` 扩展（旧配置兼容）
- 铁律审计：每个删除/裁决分支断言输家内容落盘（回收站或副本），`folder_delete_expands_tombstones` 断言三端回收站内容齐全

**前端 82/82**（M2 73 全部保留 + 9 新增）：循环状态机（开关关空转 / vault 未开空转 / 触发 A 60s 门控 / 触发 B 退避 1s→2s→4s + mDNS 重试 + 恢复跳变立即回合 / 串行锁不双发 / 触发 C 5s 尾沿重置 / 离线不参与保存触发 / backoff 封顶 / 开关持久化）；store 衔接回归（报告结构 updates）

**门禁**：`pnpm test` + `cargo test` 全绿；`scripts/android-check.sh` 交叉编译绿（每步提交后各验一次）。

### 11e. 真机走查（REDMI K90 / Android 16）

**状态（2026-09-20）**：debug APK 构建成功（`app-universal-debug.apk`，debug 签名与已装应用一致，可 `adb install -r` 覆盖）。走查结论：7 项全部通过（清单见下）；项 4 首走发现「手机目录不刷新远端删除」，§11f 修复 + 复验通过。**M3 验收通过**。

走查前桌面端准备：
1. `adb install -r src-tauri/gen/android/app/build/outputs/apk/universal/debug/app-universal-debug.apk`
2. 手机开库（演示库或真库）→ 同步中心自动起服（FGS 常驻）
3. 桌面端起 dev（`pnpm tauri dev`）→ 配对手机

§10 验收清单（逐项打勾，结论回写本节）：

- [x] **1. 三端收敛**：桌面×2 + 手机×1 各自改动（含附件），自动循环（不点任何按钮）≤3 回合三端一致
  - 备注：单桌面机环境，「桌面×2」以两个 vault（演示库 + 临时库）模拟双客户端；Rust 集成测试已覆盖等价拓扑
- [x] **2. 离线自动同步**：飞行模式 → 改 3 篇 → 恢复网络 → ≤2 个探测周期内自动同步，三端互见
- [x] **3. LWW 裁决**：双端离线改同一篇（保存时间不同）→ 同步 → 较新版两端原路径、较旧版可见冲突副本（内容完整），全程无人工操作；报告列出合并明细；次回合收敛（副本三端互见）
- [x] **4. 删除传播**：桌面删一篇 → 同步 → 手机与桌面 2 该笔记均消失（三端回收站均有）；随后从回收站恢复并编辑 → 同步 → 按 LWW 裁决，无手动决策
  - 备注：首走发现手机端目录不刷新远端删除（点击已删笔记报「笔记不存在」），§11f 修复；修复版 APK 复验通过（桌面删除 → 手机目录即时消失；同源场景桌面新建 → 手机目录即时出现）
- [x] **5. 开关**：关自动同步 → 5 分钟内无回合（日志/探测计数观测），手动按钮有效；再开 → ≤60s 恢复回合
- [x] **6. 服务器恢复**：杀手机 App 进程（FGS 重启、自动开库复活）→ 桌面探测退避、≤60s 内恢复并自动完成一轮
- [x] **7.** `pnpm test` + `cargo test` 全绿（前端 87/87 + Rust 91/91，§11f 后账目）；CI 三端绿（build #35504612769：windows/linux/android ✓）；真机走查通过（REDMI K90 / Android 16）

### 11f. 走查发现与修复：手机目录不刷新远端删除（2026-09-20 走查中）

**现象（验收项 4 场景）**：桌面删一篇笔记并同步 → 移动端该笔记在目录中仍出现，点击报「笔记不存在」。文件侧实际已正确：桌面回合经 `/delete` 把手机侧文件软删入回收站（DB 清 files/recents/favorites 三表 + 记 tombstone）。

**根因**：手机是同步服务器。服务器侧 `/push`、`/delete` 改动 vault 后**没有任何通知手机端 UI**——手机无客户端循环（设计如此，docs/07 §7），目录树只在本地操作（开库/增删改/重命名/手动重新扫描）后刷新，内存树停留在远端改动前的快照。同源隐性陈旧：远端 push 的新笔记/更新在手机上同样要等手动「重新扫描」才出现。桌面端无此问题：`runRound` 回合后自行 `refreshTree/refreshMeta`。

**修复**：

- **Rust**：`AppState.sync_notify` 槽位（Tauri setup 注册 emit 闭包，`lanmark:vault-changed` 事件）→ `ServerCtx.ui_notify`（spawn 时接线；测试直接建 ctx 时为 None，可注入计数器）。`/push`、`/delete` 有成功项时发事件，全失败不发（失败 = vault 未变，刷新无意义且会惊动 UI）
- **前端**：vault store 新增 `remoteChanged()`——刷树 + meta（DB 已由服务器侧 handler 维护，无需全量 reindex）；打开中的笔记**未在编辑**则重载内容（与桌面 runRound 同守卫，防陈旧内容被下一次输入落盘覆盖远端版）；**在编辑**（dirty）则不动内容（进行中的输入优先，保存后按 LWW 裁决）；读失败（远端已删）保留当前状态、不报错（用户编辑保存后按 edit/delete LWW 复活，§4.2）。App.tsx 监听事件；另加 **Android 回前台兜底**（同步发生在 WebView 暂停期间时事件可能丢失 → 回前台补刷，3s 防抖；桌面由自动循环触发器 D 覆盖，不加）。手机同步面板不受影响（本有 5s 轮询 `refreshPairing`）
- **测试账**：Rust +1（`push_and_delete_notify_ui_on_change`：成功 push/delete 各通知恰好一次、全失败不通知、文件落盘/入回收站断言）→ **91/91**；前端 +5（`remoteChanged`：vault 未打开无操作 / 刷新树与 meta / 打开中未编辑重载 / dirty 不动内容 / 远端删除读失败保留状态不报错）→ **87/87**；`tsc --noEmit` 通过
- **复验**：修复版 debug APK 重装后复走项 4（桌面删一篇 → 手机目录即时消失；顺带可验同源场景：桌面新建一篇 → 手机目录即时出现）→ **通过（2026-09-20）**

## 12. 已知限制与风险

- 桌面 mDNS 多播不可达（docs/05 §8h）：发现仍靠手输 URL；探测循环不依赖 mDNS，不受影响
- 60s 周期回合以 reindex 开头：M1 护栏 500 笔记 ≈22ms，外推 1e4 笔记 ≈0.5s，可接受；未来量大改 mtime 预检（P2，不阻塞 M3）
- **mtime 时钟偏移**：裁决方向可能因设备时钟偏差而「反」，但输家留副本，无丢失（§4.3）
- 平手裁决的两个客户端可能各自方向相反，依赖服务器仲裁 + 基线修复 ≤2 回合收敛（§5 步骤 5）
- 自动循环保存触发 5s 尾沿，不会打字中途同步（与 M2 P0 修复同纪律）
- 「自动合并」对用户是静默的（较旧版降级为副本）：可发现性靠副本在树 + 报告；若用户反馈困惑，二期版本历史 UI 补「合并历史」视图
